services:services:

  - type: web  - type: web

    name: task-tracker    name: task-tracker

    env: node    env: node

    plan: free    buildCommand: npm install

    buildCommand: npm install --production    startCommand: npm start

    startCommand: npm start    envVars:

    envVars:      - key: NODE_ENV

      - key: NODE_ENV        value: production
        value: production
    autoDeploy: true